//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const transformationsTitle = () => {
  const record = useRecordContext();
  return <span>transformations {record ? `"${record.id}"` : ''}</span>;
};

export const transformationsList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <TextField source="evolutionProcess" />
      <TextField source="targetCharacter" />
      <BooleanField source="suitabilityToPosition" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const transformationsEdit = () => (
  <Edit title={<transformationsTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="evolutionProcess" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="targetCharacter" />
        </Grid>
        <Grid item xs={4}>
          <BooleanInput source="suitabilityToPosition" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const transformationsCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="evolutionProcess" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="targetCharacter" />
        </Grid>
        <Grid item xs={4}>
          <BooleanInput source="suitabilityToPosition" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  ,
  ,
  ,
];
